#!/bin/bash

# Find the token secret for default account

# Make that default account cluster admin  (FIXME)

# create a configmap from virtserv.conf

# process the template

# Add a service to map to the template (port 8080)